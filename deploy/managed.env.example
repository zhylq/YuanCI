# Copy to .secrets/managed.env; do not reuse Quickstart credentials.
# Use a random hex password to avoid URL-escaping mistakes.
YUANCI_POSTGRES_PASSWORD=
YUANCI_PUBLIC_ORIGIN=https://ci.example.com
YUANCI_MANAGED_PORT=8082
